# Copy to .env.local (gitignored) and set a value.
#
# This only unlocks admin.html during local development. It is compared in the
# browser, so it is not a secret and must never protect anything that matters.
# Do not reuse a passcode you use anywhere else.
VITE_ADMIN_PASSCODE=
